package com.tianque.resourcePool.validator;

import org.springframework.stereotype.Component;

import com.tianque.core.validate.DomainValidator;
import com.tianque.core.validate.ValidateResult;
import com.tianque.resourcePool.domain.DirectorySetting;

@Component("DirectorySettingValidator")
public class DirectorySettingValidator implements DomainValidator<DirectorySetting> {

	@Override
	public ValidateResult validate(DirectorySetting domain) {
		ValidateResult result = domain.validate();
		return result;
	}

}
